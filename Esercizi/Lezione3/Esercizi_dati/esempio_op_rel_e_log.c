#include <math.h>

int main() {
    // Esempi di utilizzo degli operatori relazionali
    int a = 3 < 4; // Si legge come a = (3 < 4)
    int b = 3 >= 4;
    int c = 2 == 1;
    int d = 1 == 1;
    // Esempi di utilizzo degli operatori logici
    int e = !(2 > 3);
    int f = (1 != 2) && (3 == 4);
    int g = (1 != 2) || (3 == 4);
    return 0;
}